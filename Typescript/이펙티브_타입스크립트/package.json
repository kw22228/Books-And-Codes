{
    "name": "effective_typescipt",
    "version": "1.0.0",
    "description": "-   tsc --noImplicitAny program.ts\r -   tsc --init으로 tsconfig.json 설정 파일을 통해 설정 가능.",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "tsc": "tsc --init"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@types/express": "^4.17.15",
        "typescript": "^4.9.4"
    },
    "dependencies": {
        "express": "^4.18.2"
    }
}
