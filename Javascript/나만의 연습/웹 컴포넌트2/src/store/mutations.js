export default {
    addItem: (state, item) => {
        state.typed = item;
        // return state;
    },
    // deleteItem: (state, index) => {
    //     state.items.splice(index, 1);
    //     return state;
    // },
};
